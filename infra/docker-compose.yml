services:
  ai-service-stub:
    build: ./ai-service-stub
    ports:
      - "8090:8090"

  ai-service:
    profiles: ["llm"]
    build:
      context: ../ai-service
      dockerfile: Dockerfile
    ports:
      - "8091:8090"
    environment:
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      STT_MODEL: ${STT_MODEL:-whisper-large-v3}
      LLM_MODEL: ${LLM_MODEL:-gpt-5.6-luna}
      TTS_MODEL: ${TTS_MODEL:-gpt-4o-mini-tts}
      TTS_VOICE: ${TTS_VOICE:-coral}
      TTS_RESPONSE_FORMAT: ${TTS_RESPONSE_FORMAT:-opus}

  server:
    build:
      context: ..
      dockerfile: infra/server/Dockerfile
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: "8080"
      AI_SERVICE_BASE_URL: ${AI_SERVICE_BASE_URL:-http://ai-service-stub:8090}
    depends_on:
      - ai-service-stub
